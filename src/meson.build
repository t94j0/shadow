shadow_sources = ['shadow.cpp', 'user.cpp']
shadow = shared_library('shadow',
                        shadow_sources,
                        include_directories : inc,
                        dependencies : boost_dep,
                        install : true)
